 
name: relay_server
description: "Relay WebSocket server for Scrabble_chen"
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  shelf: ^1.4.0
  shelf_web_socket: ^1.0.1
  
  scrabble_chen:
    path: /data/flutter/scrabble_chen
  
  
startCommand: dart run bin/relay_server.dart