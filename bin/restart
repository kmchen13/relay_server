#!/bin/bash
#
# Restart the relay server via systemctl and show the last 50 log lines
#

sudo systemctl stop relay_server.service
sudo systemctl start relay_server.service
journalctl -u relay_server.service -f -n 50
